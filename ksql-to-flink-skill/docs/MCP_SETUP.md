# Flink deploy setup (superseded)

Deploy configuration moved to [FLINK_DEPLOY.md](FLINK_DEPLOY.md).

The harness uses the `confluent-sql` Python library (REST API) via `flink-skill-common`. MCP Confluent is no longer required.
